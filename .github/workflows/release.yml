name: "Prepare release"

on:
  push:
    tags:
      - "release-*"
  workflow_dispatch:
    # allows manual trigger

permissions:
  contents: "write"

env:
  THIRD_PARTY_BASE_DIR: "third-party"

jobs:
  emu-win-all:
    name: "Emu win all"
    if: ${{ !cancelled() }}
    uses: "./.github/workflows/emu-build-all-win.yml"

  emu-win-prep:
    needs: ["emu-win-all"]
    runs-on: "windows-2025-vs2026"
    steps:
      # on Windows Git will auto change line ending to CRLF, not preferable
      - name: "Ensure LF line ending"
        shell: "cmd"
        working-directory: "${{ github.workspace }}"
        run: |
          git config --local core.autocrlf false
          git config --system core.autocrlf false
          git config --global core.autocrlf false

      # we need branch because it has package scripts
      - name: "Checkout branch"
        uses: actions/checkout@v6

      - name: "Clone third-party deps (deps/win)"
        uses: actions/checkout@v6
        with:
          ref: "third-party/deps/win"
          path: "${{env.THIRD_PARTY_BASE_DIR}}/deps/win"

      # download artifacts
      - name: "Download emu build artifacts (Win)"
        uses: actions/download-artifact@v8
        with:
          path: "build/win"
          pattern: "emu-win-*-${{ github.sha }}"
          merge-multiple: true

      # print files
      - name: "Print files"
        shell: "cmd"
        working-directory: "${{ github.workspace }}"
        run: |
          dir /s /b /a:-d build\win

      # package (release mode)
      - name: "Package build (release)"
        shell: "cmd"
        working-directory: "${{ github.workspace }}"
        run: package_win.bat vs2026\release

      # package (debug mode)
      - name: "Package build (debug)"
        shell: "cmd"
        working-directory: "${{ github.workspace }}"
        run: package_win.bat vs2026\debug 1

      # release (debug + release modes) if this is a tag push
      - name: "Release"
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          files: "build/package/win/**/*"

      # upload artifacts/packages if this is a manual run
      - name: "Upload release package"
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v7
        with:
          name: "release-emu-win-release-${{ github.sha }}"
          path: "build/package/win/vs2026/*release*"
          if-no-files-found: "error"
          compression-level: 0
          retention-days: 7

      - name: "Upload debug package"
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v7
        with:
          name: "release-emu-win-debug-${{ github.sha }}"
          path: "build/package/win/vs2026/*debug*"
          if-no-files-found: "error"
          compression-level: 0
          retention-days: 7

  emu-win-all-vs22:
    name: "Emu win all vs22"
    if: ${{ !cancelled() }}
    uses: "./.github/workflows/emu-build-all-win-vs22.yml"

  emu-win-prep-vs22:
    needs: ["emu-win-all-vs22"]
    runs-on: "windows-2022"
    steps:
      # on Windows Git will auto change line ending to CRLF, not preferable
      - name: "Ensure LF line ending"
        shell: "cmd"
        working-directory: "${{ github.workspace }}"
        run: |
          git config --local core.autocrlf false
          git config --system core.autocrlf false
          git config --global core.autocrlf false

      # we need branch because it has package scripts
      - name: "Checkout branch"
        uses: actions/checkout@v6

      - name: "Clone third-party deps (deps/win)"
        uses: actions/checkout@v6
        with:
          ref: "third-party/deps/win"
          path: "${{env.THIRD_PARTY_BASE_DIR}}/deps/win"

      # download artifacts
      - name: "Download emu build artifacts (Win)"
        uses: actions/download-artifact@v8
        with:
          path: "build/win"
          pattern: "emu-win-vs22-*-${{ github.sha }}"
          merge-multiple: true

      # print files
      - name: "Print files"
        shell: "cmd"
        working-directory: "${{ github.workspace }}"
        run: |
          dir /s /b /a:-d build\win

      # package (release mode)
      - name: "Package build (release)"
        shell: "cmd"
        working-directory: "${{ github.workspace }}"
        run: package_win.bat vs2022\release

      # package (debug mode)
      - name: "Package build (debug)"
        shell: "cmd"
        working-directory: "${{ github.workspace }}"
        run: package_win.bat vs2022\debug 1

      # Rename
      - name: "Rename"
        if: startsWith(github.ref, 'refs/tags/')
        shell: "cmd"
        working-directory: "build/package/win/vs2022"
        run: |
          ren emu-win-debug.7z emu-win-debug-vs22.7z
          ren emu-win-release.7z emu-win-release-vs22.7z
          
      # release (debug + release modes) if this is a tag push
      - name: "Release"
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          files: "build/package/win/**/*"

      # upload artifacts/packages if this is a manual run
      - name: "Upload release package"
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v7
        with:
          name: "release-emu-win-vs22-release-${{ github.sha }}"
          path: "build/package/win/vs2022/*release*"
          if-no-files-found: "error"
          compression-level: 0
          retention-days: 7

      - name: "Upload debug package"
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v7
        with:
          name: "release-emu-win-vs22-debug-${{ github.sha }}"
          path: "build/package/win/vs2022/*debug*"
          if-no-files-found: "error"
          compression-level: 0
          retention-days: 7
